{% extends 'layout.html.twig' %}


{% block content %}
    <div class="container">
        <div class="row">
            {% for cocktail in cocktails.drinks %}
                <div class="col-md-6 col-lg-3 mr-5 mb-5 d-flex">
                     <div class="card" style="width: 18rem;">
                     <img src="/assets/images/martini.png" class="card-img-top" alt="...">
                        <div class="card-body">
                             <h5 class="card-title"> {{ cocktail.strCategory }}</h5>
                          <a href="#" class="btn btn-info">Voir les cocktails</a>
                     </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}